# API backend (desarrollo local — puerto 3000)
VITE_API_URL=http://localhost:3000/api

# true = mocks + localStorage | false = backend real
VITE_USE_MOCKS=false

# El dev server de Vite usa puerto fijo 5173 (strictPort). Abre http://localhost:5173
